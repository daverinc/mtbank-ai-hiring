# === Core ===
fastapi
uvicorn[standard]
python-dotenv
pydantic
requests
python-multipart

# === ASR ===
faster-whisper
pyannote.audio

# Примечание:
# PyTorch (torch + torchaudio) устанавливается отдельно в Dockerfile
# с CPU-версией через:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# === Multi-Agent + LLM ===
langgraph
langchain-core
groq
openai

# === Utils ===
jiwer
pydub